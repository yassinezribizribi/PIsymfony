<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paiement - {{ cours.titreCours }}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4 text-center text-primary">Payer pour le cours : {{ cours.titreCours }}</h1>

        <div class="row">
            <div class="col-md-6">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h4 class="card-title text-success">Détails du cours</h4>
                        <p><strong>Description :</strong> {{ cours.descriptionCours }}</p>
                        <p><strong>Prix :</strong> {{ cours.prix }} €</p>
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <h4 class="text-info">Informations de paiement</h4>
                <!-- Formulaire de paiement -->
                <form id="payment-form" method="POST" action="#">
                    <div class="form-group">
                        <label for="cardName">Nom sur la carte :</label>
                        <input type="text" id="cardName" class="form-control" placeholder="John Doe" required>
                    </div>
                    <div class="form-group">
                        <label for="cardNumber">Numéro de carte :</label>
                        <input type="text" id="cardNumber" class="form-control" placeholder="XXXX XXXX XXXX XXXX" required>
                    </div>
                    <div class="form-group">
                        <label for="expiryDate">Date d'expiration :</label>
                        <input type="text" id="expiryDate" class="form-control" placeholder="MM/AA" required>
                    </div>
                    <div class="form-group">
                        <label for="cvv">Code CVV :</label>
                        <input type="text" id="cvv" class="form-control" placeholder="XXX" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Adresse email :</label>
                        <input type="email" id="email" class="form-control" placeholder="email@example.com" required>
                    </div>
                    <button type="submit" class="btn btn-success btn-block">Procéder au paiement</button>
                </form>
            </div>
        </div>

        <!-- Message de confirmation ou d'erreur -->
        <div id="paymentMessage" class="mt-4 text-center" style="display: none;">
            <div id="successMessage" class="alert alert-success" style="display: none;">
                <strong>Succès !</strong> Votre paiement a été effectué avec succès.
            </div>
            <div id="errorMessage" class="alert alert-danger" style="display: none;">
                <strong>Erreur !</strong> Une erreur est survenue. Veuillez vérifier vos informations.
            </div>
        </div>

        <div class="mt-4 text-center">
            <a href="{{ path('front_cours_index') }}" class="btn btn-primary">Retour à la liste des cours</a>
        </div>
    </div>

    <script>
        document.getElementById('payment-form').addEventListener('submit', function(event) {
            event.preventDefault();

            // Récupérer les données saisies
            const cardName = document.getElementById('cardName').value;
            const cardNumber = document.getElementById('cardNumber').value;
            const expiryDate = document.getElementById('expiryDate').value;
            const cvv = document.getElementById('cvv').value;

            // Validation basique pour simuler le paiement
            if (cardNumber === '4111111111111111' && expiryDate === '12/23' && cvv === '123') {
                // Si les informations sont correctes (paiement réussi)
                showPaymentMessage(true);
            } else {
                // Sinon, paiement échoué
                showPaymentMessage(false);
            }
        });

        function showPaymentMessage(success) {
            document.getElementById('paymentMessage').style.display = 'block';
            document.getElementById('successMessage').style.display = success ? 'block' : 'none';
            document.getElementById('errorMessage').style.display = success ? 'none' : 'block';
        }
    </script>
</body>
</html>
